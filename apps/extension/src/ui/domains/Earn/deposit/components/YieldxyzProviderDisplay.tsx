// import { FC } from "react"

// import { useYieldxyzProvider } from "@ui/state/yield"

// export const YieldxyzProviderDisplay: FC<{ providerId: string; className?: string }> = ({
//   providerId,
//   className,
// }) => {
//   const { data: provider } = useYieldxyzProvider(providerId)

//   if (!provider) return null

//   return <YieldxyzProviderDisplay providerId={providerId} className={className} />
// }
