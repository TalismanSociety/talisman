# @talismn/token-rates

Fiat and crypto prices for tokens:

- `fetchTokenRates` fetches rates from the Talisman coins API (`coins.talisman.xyz`) for the supported currencies (`ALL_CURRENCY_IDS`)
- `getDTaoTokenRates` computes rates for Bittensor subnet (dTAO) tokens
